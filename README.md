# Simulation-project
This C program takes marks of students as input, then calculates and displays the highest, lowest, and average marks. It also allows the user to search for a specific mark and reports its position if found.
